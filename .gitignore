# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables — only .env (non-secrets) is committed
.env.local
.env.*.local
.env.production
.env.preview

# local test keypair (never commit private keys)
dev/test-private.pem

# macOS-specific files
.DS_Store
